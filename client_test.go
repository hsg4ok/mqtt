package mqtt

import (
    "testing"
)

func TestMQTTConnectPublic(t *testing.T) {
/*
    cs := ClientSocket{}

    err := cs.Connect("mqtt://test.mosquitto.org")
    if err != nil {
        t.Error("could not connect", err)
    }
 */   
}
